# 空表数据分析报告

**生成日期**: 2026-04-21  
**当前分支**: `feature/qwen2api-jimeng-support`  
**数据来源**: `docs/public.sql`  
**最后更新**: 2026-04-21 (修复 o_event/o_eventChapter 保存逻辑)

---

## 一、空表清单

根据 public.sql 导出文件统计，以下 **7 张表** 目前为空（无 INSERT 语句）：

| 序号 | 表名 | 预期用途 | 状态 |
|------|------|---------|------|
| 1 | o_artStyle | 艺术风格配置 | ⚠️ 可选 - 功能未使用 |
| 2 | o_modelPrompt | 模型提示词绑定 | ⚠️ 可选 - 功能未使用 |
| 3 | o_skillAttribution | 技能属性 | ⚠️ 可选 - 技能系统未启用 |
| 4 | o_skillList | 技能列表 | ⚠️ 可选 - 技能系统未启用 |
| 5 | o_assetsRole2Audio | 角色 - 音频关联 | ⚠️ 可选 - TTS 功能未执行 |
| 6 | o_imageFlow | 图片编辑流程 | ⚠️ 可选 - 图片编辑功能未使用 |
| 7 | ~~o_event~~ | ~~事件数据~~ | ✅ **已修复** - 事件生成后正常写入 |
| 8 | ~~o_eventChapter~~ | ~~事件章节~~ | ✅ **已修复** - 事件生成后正常写入 |

**修复记录**:
- 2026-04-21: 修复 `o_event`/`o_eventChapter` 保存逻辑（PostgreSQL 插入返回值问题）
- 2026-04-21: 修复 `o_prompt` 表初始化逻辑（insert-or-update 模式）

---

## 二、详细分析

### 2.1 ✅ 已修复（曾有数据但缺失）

#### ~~o_event（事件表） & o_eventChapter（事件章节表）~~

**状态**: 已修复，事件生成后正常写入数据

**修复内容**:
- **问题**: PostgreSQL 的 `insert` 返回值格式与 SQLite 不同，导致 `eventId` 为 `undefined`
- **修复**: 在 [`src/utils/cleanNovel.ts`](src/utils/cleanNovel.ts#L95-L103) 添加 `.returning("id")` 获取插入后的 ID
- **验证**: 调用 `/api/novel/event/generateEvents` 接口后，两表均正常写入数据

---

### 2.2 ⚠️ 可选数据（功能未启用/设计如此）

#### o_artStyle（艺术风格表）

#### o_artStyle（艺术风格表）

**用途**: 存储预设的艺术风格配置

**使用位置**:
- [`src/routes/artStyle/getArtStyle.ts`](src/routes/artStyle/getArtStyle.ts) - 获取风格列表
- [`src/routes/artStyle/addArtStyle.ts`](src/routes/artStyle/addArtStyle.ts) - 添加风格
- [`src/routes/artStyle/editArtStyle.ts`](src/routes/artStyle/editArtStyle.ts) - 编辑风格

**为什么为空**:
- 项目可能使用硬编码的默认风格
- 或者用户在创建项目时直接输入风格描述，而非选择预设风格

**是否需要补充**: 可选。如果前端有风格选择功能，建议添加一些默认风格（如：3D 动画、赛璐珺、写实风格等）。

---

#### o_modelPrompt（模型提示词绑定表）

**用途**: 为不同模型绑定特定的提示词

**使用位置**:
- [`src/routes/setting/modelMap/getImageAndVideoModel.ts`](src/routes/setting/modelMap/getImageAndVideoModel.ts) - 获取模型列表及绑定的 prompt
- [`src/routes/setting/modelMap/bindingPrompt.ts`](src/routes/setting/modelMap/bindingPrompt.ts) - 绑定 prompt 到模型

**为什么为空**:
- 可能使用通用 prompt，未针对特定模型做优化
- 或者模型 prompt 功能未启用

**是否需要补充**: 可选。如果需要对不同模型（如 Jimeng、Qwen2API）使用不同的 prompt，建议配置。

---

#### o_prompt（系统提示词管理表）

**状态**: ✅ **已修复** - fixDB 中使用 insert-or-update 模式初始化

**修复内容**:
- **问题**: fixDB 中只有 `update` 没有 `insert`，空表时无法初始化
- **修复**: 在 [`src/lib/fixDB.ts`](src/lib/fixDB.ts#L403-L430) 改为先检查后插入的模式
- **验证**: 重启服务后自动插入 3 条默认 prompt（eventExtraction、scriptAssetExtraction、videoPromptGeneration）

---

**用途**: 存储预设的艺术风格配置

**使用位置**:
- [`src/routes/artStyle/getArtStyle.ts`](src/routes/artStyle/getArtStyle.ts) - 获取风格列表
- [`src/routes/artStyle/addArtStyle.ts`](src/routes/artStyle/addArtStyle.ts) - 添加风格
- [`src/routes/artStyle/editArtStyle.ts`](src/routes/artStyle/editArtStyle.ts) - 编辑风格

**为什么为空**:
- 项目可能使用硬编码的默认风格
- 或者用户在创建项目时直接输入风格描述，而非选择预设风格

**是否需要补充**: 可选。如果前端有风格选择功能，建议添加一些默认风格（如：3D 动画、赛璐珞、写实风格等）。

---

#### o_modelPrompt（模型提示词绑定表）

**用途**: 为不同模型绑定特定的提示词

**使用位置**:
- [`src/routes/setting/modelMap/getImageAndVideoModel.ts`](src/routes/setting/modelMap/getImageAndVideoModel.ts) - 获取模型列表及绑定的 prompt
- [`src/routes/setting/modelMap/bindingPrompt.ts`](src/routes/setting/modelMap/bindingPrompt.ts) - 绑定 prompt 到模型

**为什么为空**:
- 可能使用通用 prompt，未针对特定模型做优化
- 或者模型 prompt 功能未启用

**是否需要补充**: 可选。如果需要对不同模型（如 Jimeng、Qwen2API）使用不同的 prompt，建议配置。

---

#### o_prompt（系统提示词管理表）

**用途**: 存储系统级提示词模板

**使用位置**:
- [`src/utils/cleanNovel.ts`](src/utils/cleanNovel.ts#L30) - 使用 `eventExtraction` 类型的 prompt
- [`src/routes/script/extractAssets.ts`](src/routes/script/extractAssets.ts#L205) - 使用 `scriptAssetExtraction` 类型的 prompt
- [`src/routes/production/workbench/generateVideoPrompt.ts`](src/routes/production/workbench/generateVideoPrompt.ts#L84) - 使用 `videoPromptGeneration` 类型的 prompt
- [`src/routes/setting/promptManage/getPrompt.ts`](src/routes/setting/promptManage/getPrompt.ts) - 获取所有 prompt
- [`src/routes/setting/promptManage/updatePrompt.ts`](src/routes/setting/promptManage/updatePrompt.ts) - 更新 prompt

**为什么为空**:
代码中使用了 `o_prompt` 表查询 prompt，但表中没有数据。这可能意味着：
1. prompt 可能硬编码在代码中
2. 或者 `fixDB.ts` 中有初始化逻辑但未执行

**检查 fixDB.ts**:
```typescript
// src/lib/fixDB.ts:403-406
await db("o_prompt").where("type", "scriptAssetExtraction").update({...});
await db("o_prompt").where("type", "videoPromptGeneration").update({...});
```

fixDB 中确实有 o_prompt 的初始化逻辑，但可能未执行成功。

**是否需要补充**: **建议补充**。如果代码逻辑依赖这些 prompt，应该确保表中有默认数据。

---

#### o_skillList（技能列表） & o_skillAttribution（技能属性表）

**用途**: 存储 Agent 技能配置

**表结构**:
```sql
-- o_skillList
id, name, description, content, created_at, updated_at

-- o_skillAttribution
id, skillId, type, value, created_at
```

**使用位置**:
- [`src/routes/setting/skillManagement/getSkillList.ts`](src/routes/setting/skillManagement/getSkillList.ts) - 获取技能列表（从文件系统读取）
- [`src/routes/setting/skillManagement/getSkillContent.ts`](src/routes/setting/skillManagement/getSkillContent.ts) - 获取技能内容

**为什么为空**:
- **技能配置存储在文件系统** (`data/skills/` 目录)，而非数据库
- `getSkillList.ts` 使用 `fast-glob` 从文件系统读取 `**/*.md` 文件
- 数据库表设计可能未启用或已废弃

**是否需要补充**: 不需要。技能系统使用文件系统存储，数据库表可能已废弃。

---

#### o_assetsRole2Audio（角色 - 音频关联表）

**用途**: 存储角色配音关联

**表结构**:
```sql
id, assetsRoleId, assetsAudioId, audioPath, created_at
```

**使用位置**:
- [`src/routes/cornerScape/getAllAssets.ts`](src/routes/cornerScape/getAllAssets.ts#L35-47) - 查询角色资产时关联音频
- [`src/routes/cornerScape/updateAssetsAudio.ts`](src/routes/cornerScape/updateAssetsAudio.ts) - 更新角色音频
- [`src/routes/cornerScape/batchBindAudio.ts`](src/routes/cornerScape/batchBindAudio.ts) - 批量绑定音频

**为什么为空**:
- TTS 配音功能可能未执行
- 或者当前流程只生成图片/视频，未生成音频

**是否需要补充**: 可选。如果 TTS 配音功能未使用，可以保持为空。

---

#### o_imageFlow（图片编辑流程表）

**用途**: 存储图片编辑历史/流程数据

**表结构**:
```sql
id, imageId, flowType, flowData, created_at
```

**使用位置**:
- [`src/routes/production/editImage/getImageFlow.ts`](src/routes/production/editImage/getImageFlow.ts) - 获取图片编辑流程
- [`src/routes/production/editImage/saveImageFlow.ts`](src/routes/production/editImage/saveImageFlow.ts) - 保存编辑流程
- [`src/routes/production/editImage/updateImageFlow.ts`](src/routes/production/editImage/updateImageFlow.ts) - 更新编辑流程

**为什么为空**:
- 图片编辑功能可能未使用
- 或者当前流程只生成图片，未进行二次编辑

**是否需要补充**: 可选。如果图片编辑功能未使用，可以保持为空。

---

## 三、总结与建议

### 3.1 ✅ 已修复的数据

| 表名 | 原因 | 修复操作 |
|------|------|---------|
| o_event / o_eventChapter | PostgreSQL insert 返回值问题 | 添加 `.returning("id")` 获取插入 ID |
| o_prompt | fixDB 只有 update 没有 insert | 改为 insert-or-update 模式 |

### 3.2 ⚠️ 可选补充的数据（功能未启用）

| 表名 | 原因 | 建议操作 |
|------|------|---------|
| o_event / o_eventChapter | 小说→剧本流程中间环节缺失 | 检查事件提取 Agent 调用逻辑 |
| o_prompt | 代码中多处查询但表为空 | 在 fixDB 或 initPg 中添加默认 prompt 数据 |

### 3.2 ⚠️ 可选补充的数据（功能未启用）

| 表名 | 用途 | 状态 | 建议 |
|------|------|------|------|
| o_artStyle | 预设艺术风格 | 功能存在但未使用 | 如需风格选择功能可添加默认风格 |
| o_modelPrompt | 模型专属 prompt | 功能存在但未使用 | 如需模型优化可配置 |
| o_assetsRole2Audio | TTS 配音关联 | TTS 功能未执行 | 启用 TTS 功能后再补充 |
| o_imageFlow | 图片编辑历史 | 图片编辑功能未使用 | 启用图片编辑功能后再补充 |
| o_skillList / o_skillAttribution | Agent 技能 | **使用文件系统存储** | 数据库表可能已废弃，无需补充 |

### 3.3 数据流程完整性

| 表名 | 用途 | 建议 |
|------|------|------|
| o_artStyle | 预设艺术风格 | 添加常用风格（3D 动画、赛璐珞、写实等） |
| o_modelPrompt | 模型专属 prompt | 为常用模型配置优化 prompt |
| o_assetsRole2Audio | TTS 配音 | 如果启用 TTS 功能再补充 |
| o_imageFlow | 图片编辑历史 | 如果启用图片编辑功能再补充 |
| o_skillList / o_skillAttribution | Agent 技能 | **使用文件系统存储** | 数据库表可能已废弃，无需补充 |

### 3.3 数据流程完整性

当前流程状态：

```
项目创建 (o_project:1) ✓
  ↓
小说录入 (o_novel:1) ✓
  ↓
事件提取 (o_event:✓) ✅ 已修复
  ↓
剧本生成 (o_script:1) ✓
  ↓
分镜生成 (o_storyboard:28) ✓
  ↓
素材生成 (o_assets:14) ✓
  ↓
图片生成 (o_image:20) ✓
  ↓
图片编辑 (o_imageFlow:0) ⚪ ← 可选功能
  ↓
视频生成 (o_video:1) ⚠️ ← 仅 5 秒，未完成
  ↓
TTS 配音 (o_assetsRole2Audio:0) ⚪ ← 可选功能
```

---

**报告结束**
